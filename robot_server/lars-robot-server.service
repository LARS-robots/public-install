[Unit]
Description=LARS Robot Server (API + Camera Daemon + Motion Daemon)
After=network-online.target docker.service
Wants=network-online.target docker.service

[Service]
Type=simple
User={{SERVICE_USER}}
Group={{SERVICE_USER}}
WorkingDirectory={{INSTALL_DIR}}
Environment=PYTHONUNBUFFERED=1
Environment=PYTHONPATH={{INSTALL_DIR}}/services/api/src
Environment=GPIOZERO_PIN_FACTORY=lgpio
Environment=API_TAG=dev
# Load robot type from env file (written by unpack.py)
EnvironmentFile=/etc/lars/robot.env

# Start all services
ExecStart=/usr/bin/python3 {{INSTALL_DIR}}/scripts/compose_service.py up {{INSTALL_DIR}}

# Stop all services
ExecStop=/usr/bin/python3 {{INSTALL_DIR}}/scripts/compose_service.py down {{INSTALL_DIR}}

Restart=on-failure
RestartSec=10
StandardOutput=journal
StandardError=journal
KillMode=mixed
KillSignal=SIGTERM
TimeoutStopSec=30

[Install]
WantedBy=multi-user.target