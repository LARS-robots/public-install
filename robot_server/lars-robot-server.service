[Unit]
Description=LARS Robot Server (API + Camera Daemon)
After=network-online.target docker.service
Wants=network-online.target docker.service

[Service]
Type=simple
User={{SERVICE_USER}}
Group={{SERVICE_USER}}
WorkingDirectory={{INSTALL_DIR}}
Environment=PYTHONUNBUFFERED=1
Environment=PYTHONPATH={{INSTALL_DIR}}/services/api/src
Environment=GPIOZERO_PIN_FACTORY=lgpio

# Start: run install.py which handles docker compose
ExecStartPre=/home/master/LARS/.venv/bin/python /home/master/LARS/scripts/install.py --tag dev --write-env
ExecStart=/usr/bin/docker compose -f /home/master/LARS/compose.yml up --build
ExecStop=/usr/bin/docker compose -f /home/master/LARS/compose.yml down

# Restart policy
Restart=on-failure
RestartSec=10

# Logging
StandardOutput=journal
StandardError=journal

# Shutdown behavior
KillMode=mixed
KillSignal=SIGTERM
TimeoutStopSec=30

[Install]
WantedBy=multi-user.target