[Unit]
Description=LARS Robot Server (API + Camera Daemon + Motion Daemon)
After=network-online.target docker.service
Wants=network-online.target docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
User={{SERVICE_USER}}
Group={{SERVICE_USER}}
WorkingDirectory={{INSTALL_DIR}}
Environment=PYTHONUNBUFFERED=1
Environment=PYTHONPATH={{INSTALL_DIR}}/services/api/src
Environment=GPIOZERO_PIN_FACTORY=lgpio
Environment=API_TAG=dev
# Load robot type from env file (written by unpack.py)
EnvironmentFile=/etc/lars/robot.env

# Start all services (action must be first: up, then root_dir, then --dev)
ExecStart=/usr/bin/python3 {{INSTALL_DIR}}/scripts/compose_service.py up {{INSTALL_DIR}} --dev

# Stop all services (action must be first: down, then root_dir, then --dev)
ExecStop=/usr/bin/python3 {{INSTALL_DIR}}/scripts/compose_service.py down {{INSTALL_DIR}} --dev

Restart=on-failure
RestartSec=10
StandardOutput=journal
StandardError=journal

# Shutdown behavior
KillMode=mixed
KillSignal=SIGTERM
TimeoutStopSec=30

[Install]
WantedBy=multi-user.target